import { JsonFormatterClient } from "./client"

export const metadata = {
    title: "JSON Formatter & Validator | API Debug Toolkit",
    description: "Free online JSON Formatter, Validator, and Minifier. Debug your API responses with ease.",
}

export default function Page() {
    return <JsonFormatterClient />
}
